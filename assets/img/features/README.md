# Feature-section characters / elements

Characters or decorative elements for the two sections you screenshotted.

⚠️ I couldn't see the two screenshots in your message, so the slots below are generic.
Tell me what each section is (or re-send the screenshots) and I'll rename these and
position each element correctly.

## Files
| Key         | Section                          |
|-------------|----------------------------------|
| `feature-1` | First screenshotted section      |
| `feature-2` | Second screenshotted section     |

→ e.g. `feature-1.png`, `feature-2.webp`

## Specs
- **Transparent PNG/WebP** recommended so they sit cleanly over the section background.
- Size depends on the section — **~600 × 600** is a safe default.

Files map into `assets/img/manifest.json` under `"features"`.
